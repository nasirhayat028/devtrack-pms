apiVersion: v1
kind: Secret

metadata:
  name: <replace me>
  namespace: devtrack

type: Opaque

stringData:
  JWT_SECRET: "<replace-me>"

  MONGO_USERNAME: "<replace-me>"
  MONGO_PASSWORD: "<replace-me>"

  MONGO_URI: "<replace-me>"

